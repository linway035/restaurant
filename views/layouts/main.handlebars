<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" />
  {{!
  <link rel="stylesheet" href="/stylesheets/bootstrap.css" /> 這效果失敗，猜測版本有差 }}
  {{!
  <link rel="stylesheet" href="/stylesheets/global.css" /> 目前將index show.css分別用head加入至各hbs似乎可行}}
  <script src="https://kit.fontawesome.com/52c9e592a9.js" crossorigin="anonymous"></script>
  <title>餐廳清單</title>
</head>

<body>
  <header>
    <!-- navigation -->
    <nav class="navbar fixed-top navbar-light bg-light">
      <div class="container">
        <a class="navbar-brand" href="/">
          <img src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png"
            width="30" height="30" class="d-inline-block align-top" alt="我的餐廳清單" />
          {{#if isAuthenticated}}{{ user.name}}的 {{/if}} 餐廳清單
        </a>
        {{#if isAuthenticated}}
        <a href="/users/logout" class="btn btn-warning">Logout</a>
        {{else}}
        <a href="/users/login" class="btn btn-warning">Login</a>
        {{/if}}
      </div>
    </nav>

    <div class="banner"></div>
  </header>
  <!-- partial templates will replace the part of "body" here -->
  {{{body}}}
  <!-- include javascript files here -->
  <script src="/javascripts/popper.js"></script>
  <script src="/javascripts/bootstrap.js"></script>
</body>

</html>